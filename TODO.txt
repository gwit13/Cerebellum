Key Features ==========

- Change "CriteriaList" into "EventList"
    - An event could be a criterion evaluation, a change in PSU settings, a
        Tamalero script, a DAQ waveform retrieval, an interlock trigger, etc.
    - We will likely only have a few criterion types, so consider refactoring
        Criterion into individual classes (e.g. PSUVoltageCriterion)
    - Modify runTest to run a main loop on this EventList
- Add PSU dependencies (e.g. PSU X must be enabled before PSU Y)
    - Config stored in EnvironmentConfig
    - Handled by _PSU in EnvironmentControl
    - Best to measure actual output voltage rather than trust setting
- Add Tamalero tests
    - Settings stored in TestSettings (under Criterion)
    - Handled by new class in EnvironmentControl
    - Will require calling existing Tamalero scripts
    - Monitor/measure output and eval PASS/FAIL?
- Add interlock + DAQ tests
    - Config stored in EnvironmentConfig (under new classes)
    - Settings stored in TestSettings (under Criterion)
    - Handled by new class in EnvironmentControl
    - Will require calling custom scripts for controlling interlock, due to the
        unlimited variety of methods for toggling a physical pin
- Add support for PSU control over AnaGate CAN
    - For us, specifically AnaGate CAN over IP
    - i.e. protocol = "IP", interface = "AnaGate (CAN)"
- Add RB module enable/disable
    - What if you wanted to check the current consumption when a different # of
        modules are selected? Would be nice to automate this
    - Would probably require writing custom Tamalero scripts
- TBD: Add cold box control
    - Will again require calling custom scripts to keep things generalized

QoL Features ==========

- EnvironmentControl: Add log verbosity
- EnvironmentControl: Add log-to-file
- Add a GUI!
